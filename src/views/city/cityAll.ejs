<!DOCTYPE html>
<html>
<head>
	<title>Cities</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js">
	</script>
	<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
	<link rel="stylesheet" href="../css/main.css">


</head>
<body>
    <%- include('../supportingLayouts/navbar', {condition, nameOfUser}); -%>
	<div class="col-md-4 col-md-offset-4">
		<div><br/>
			<h2>Cities
			</h2>
		</div>
        <div ></br>  
            <% if (cities.length > 0) { %>       
                <table class="table table-hover">
                <thead>
                    <tr>
                    <th>Անվանում</th>
                    <th>Название</th>
                    <th>Name</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(var i = 0; i < cities.length; i++) { %>
                        <tr onclick="redirect('<%= cities[i]._id %>');">
                        <td><%= cities[i].nameAM %></td>
                        <td><%= cities[i].nameRU %></td>
                        <td><%= cities[i].nameEN %></td>
                        </tr>
                    <% } %>
                </tbody>
                </table>
            <% } else { %>
                <h3>There arn't any cities yet.</h3>
            <% } %>
          </div>

		<div class="mssg bg-danger">
			<span id="check">  <%= msgErr %>  </span>
		</div>
    </div>
    <script>
        function redirect(uri) {
            window.location = window.location.toString().replace('/all', '/?id='+ uri)
        }
    </script>
</body>
</html>